# Selah Web Application - Environment Variables
# 이 파일을 .env.local로 복사한 후 실제 값을 입력하세요.
# cp .env.example .env.local

# ===========================================
# API Server
# ===========================================
# 백엔드 API 서버 주소 (CORS로 직접 통신)
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080

# ===========================================
# Note
# ===========================================
# OAuth 인증은 백엔드에서 처리됩니다.
# OAuth Client ID/Secret은 백엔드(selah-api-server)에서 설정하세요.
